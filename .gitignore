node_modules/
.next/
out/
build/
.env*
!.env.example
*.tsbuildinfo
next-env.d.ts
.DS_Store

# CMS content + uploads (never committed; lives outside the app in prod)
/data/

# Client feedback drop (source PDFs, EPS, raw fonts) — not part of the app
/client-changes/
